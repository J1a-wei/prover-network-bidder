version: "2"
sql:
- schema: "./schema.sql"
  queries: "./queries.sql"
  engine: "postgresql"
  gen:
    go:
      package: "dal"
      out: "."
      emit_json_tags: true
      json_tags_case_style: "snake"
